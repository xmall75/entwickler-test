'use strict';

const baseSeeder = {
  createRecord(record) {
    return {
      ...record,
    };
  },

  updateRecord(record) {
    return {
      ...record,
    };
  },

  deleteRecord(record) {
    return {
      ...record,
    };
  },

  readRecord(record) {
    return {
      ...record,
    };
  },
};

module.exports = baseSeeder;
